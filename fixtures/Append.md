# Tests the `+path/file` capability.

This markdown file should be processed after `./Example.md` for correct operation.

```yml >>sub/append.yml
Section 2:
  This section was created by Append.md
```